# 个人工作生活专属 APP 开发计划

> 依据文档：`PRD.md`  
> 计划状态：待执行  
> 产品形态：单用户、电脑浏览器、本机数据、无需登录和互联网  
> 建议代码目录：`E:\codexproject\coke\personal-workbench`  
> 说明：本计划只定义开发步骤，不创建应用代码。执行开发前应以本计划和 `PRD.md` 共同作为范围基准。

## 1. 执行目标和完成定义

### 1.1 最终交付物

第一版完成时必须交付：

- 一个可以在这台电脑主流浏览器中打开的个人工作生活应用。
- 首页总览、今日计划和八个专项业务模块。
- 所有模块各自适用的数据、视图和操作。
- 快速备忘转换为今日事项或专项模块记录的能力。
- 今日计划与来源模块双向同步、没有重复行动数据。
- 本地持久化、完整备份导出和整份恢复能力。
- 通过 `PRD.md` 中 `AC-01` 至 `AC-32` 的验收记录。
- 可直接运行的应用构建文件、启动说明和测试说明。
- 不依赖登录、外部接口、云数据库和正式部署。

### 1.2 全局完成定义

每个功能只有同时满足以下条件才算完成：

1. 功能符合 `PRD.md` 中对应模块的数据和操作要求。
2. 新增和修改的数据真实写入本地持久化存储。
3. 刷新页面后数据、状态和筛选条件不会异常丢失。
4. 模块摘要和今日计划读取真实数据，不使用演示数据。
5. 运行时不主动访问任何外部网络服务。
6. 有对应的单元测试、组件测试或端到端测试。
7. 桌面常用尺寸下没有遮挡、溢出或无法点击的问题。
8. 相关 `PRD.md` 验收项可以按步骤复现并通过。

### 1.3 预计工作量

按单人开发并由开发代理辅助实现估算：

| 阶段 | 预计工作量 |
| --- | --- |
| 准备和持久化验证 | 1 至 2 个工作日 |
| 基础架构和公共框架 | 2 至 3 个工作日 |
| 今日计划、快速备忘和首页核心 | 2 至 3 个工作日 |
| 第一批工作模块 | 3 至 5 个工作日 |
| 第二批生活模块 | 3 至 5 个工作日 |
| 第三批数据和知识模块 | 3 至 4 个工作日 |
| 统一联动、备份恢复和缺陷修复 | 3 至 5 个工作日 |
| 完整验收和交付整理 | 2 至 3 个工作日 |

预计总量为 19 至 30 个工作日。估算不包含等待需求变更的时间，也不因使用 AI 辅助而压缩验收和修复时间。

## 2. 已确定的技术方向

### 2.1 技术选型

| 项目 | 选择 | 原因 |
| --- | --- | --- |
| 前端 | React + TypeScript | 适合多模块、表单、状态和长期维护 |
| 构建工具 | Vite | 本地启动快，便于单元测试和构建 |
| 路由 | Hash Router | 不依赖服务器路由回退，适合本机打开 |
| UI 基础 | Ant Design 5 | 桌面表格、表单、日期、弹窗和筛选能力成熟 |
| 图标 | Lucide React | 统一工具图标，避免手工绘制 |
| 本地数据库 | Dexie + IndexedDB | 适合多张表、结构化查询和版本升级 |
| 响应式数据 | dexie-react-hooks | 数据变化后首页和模块自动刷新 |
| 日期处理 | dayjs | 体积小，和 Ant Design 日期组件兼容 |
| 备份校验 | Zod | 恢复前严格校验文件结构和版本 |
| 单元测试 | Vitest | 和 Vite 集成简单 |
| 组件测试 | React Testing Library | 验证真实用户操作 |
| 本地数据库测试 | fake-indexeddb | 在测试中验证仓储和迁移 |
| 端到端测试 | Playwright | 验证刷新、重开、下载、导入和完整流程 |
| 代码质量 | ESLint + Prettier | 统一格式并提前发现常见错误 |

### 2.2 运行方式

- 开发时使用本机开发服务器。
- 交付时构建为不依赖外部资源的本机网页应用。
- 路由使用 Hash Router，避免打开静态文件时出现路径 404。
- 如果单一网页文件无法在目标浏览器可靠使用 IndexedDB，则使用本机回环地址启动器，不使用互联网服务。
- 目标浏览器至少覆盖最新的 Chrome 和 Edge，并在其中选择一个完成最终验收。
- 不在页面中引用 CDN、在线字体、在线图标或任何外部接口。

### 2.3 数据保存原则

- IndexedDB 是应用正常运行时的主存储。
- 备份文件是用户主动导出的第二份本机数据。
- 页面刷新、关闭和浏览器重开不能清空主存储。
- 数据结构使用整数版本号，例如 `schemaVersion: 1`。
- 所有升级必须可重复执行，并通过旧版本数据迁移测试。
- 日期字段使用本地日期字符串 `YYYY-MM-DD`，时间戳使用 ISO 格式。
- 所有记录使用不可重复的 ID，例如浏览器的 `crypto.randomUUID()`。

## 3. 应用代码结构

未来应用建议放在：

```text
E:\codexproject\coke\personal-workbench\
```

建议目录划分：

```text
personal-workbench/
  src/
    app/                 # 应用入口、路由、全局配置
    components/          # 通用按钮、空状态、确认框、表单抽屉
    features/
      dashboard/         # 首页
      today/             # 今日计划
      quick-notes/       # 快速备忘
      self-media/        # 自媒体
      development/       # 开发工作
      consulting/        # 咨询工作
      fitness/           # 健身计划
      diet/              # 饮食计划
      games/             # 游戏娱乐
      data-design/       # 数据与设计
      learning/          # 学习计划
      backup/            # 数据与备份
    db/                  # Dexie 数据库、表、迁移
    domain/              # 领域类型、校验规则、状态常量
    services/            # 今日计划、摘要、转换、备份服务
    styles/              # 全局样式和桌面布局
    test/                # 测试工具和测试数据工厂
  tests/
    e2e/                 # Playwright 端到端测试
    fixtures/            # 合法和损坏的备份样本
  README.md
  package.json
```

禁止把业务逻辑直接写进页面组件。页面负责展示和触发操作，数据读写必须通过仓储或服务完成。

## 4. 数据模型

### 4.1 核心表

| 表 | 用途 | 关键字段 |
| --- | --- | --- |
| `meta` | 应用版本、数据版本和初始化状态 | `id`, `schemaVersion`, `createdAt` |
| `quickNotes` | 快速备忘 | `id`, `text`, `status`, `createdAt`, `convertedTo` |
| `standaloneTasks` | 不属于任何专项模块的今日独立事项 | `id`, `title`, `plannedDate`, `priority`, `status` |

### 4.2 自媒体

| 表 | 用途 |
| --- | --- |
| `selfMediaItems` | 选题、写作、待发布、已发布和复盘内容 |
| `selfMediaMetrics` | 已发布内容的手动表现数据 |

### 4.3 开发工作

| 表 | 用途 |
| --- | --- |
| `devProjects` | 开发项目 |
| `devItems` | 开发任务或缺陷 |
| `workLogs` | 开发工作日期、时长和说明 |

### 4.4 咨询工作

| 表 | 用途 |
| --- | --- |
| `consultingClients` | 客户 |
| `consultingProjects` | 咨询项目 |
| `consultingMeetings` | 会议和会议记录 |
| `consultingActions` | 行动项和跟进 |
| `consultingTimeEntries` | 咨询时长和可选费用 |

### 4.5 健身计划

| 表 | 用途 |
| --- | --- |
| `fitnessPlans` | 每周训练安排 |
| `workoutSessions` | 某次计划或实际训练 |
| `workoutEntries` | 动作、组数、次数、重量和时长 |
| `bodyMetrics` | 体重、围度和其他身体指标 |

### 4.6 饮食计划

| 表 | 用途 |
| --- | --- |
| `mealPlans` | 按日期和餐次保存饮食安排 |
| `mealLogs` | 实际饮食记录 |
| `waterLogs` | 每日饮水量 |
| `shoppingItems` | 食材和购物清单 |

### 4.7 游戏娱乐

| 表 | 用途 |
| --- | --- |
| `games` | 游戏名称、平台、状态、评分、感想 |
| `playSessions` | 游玩日期、时长、进展和备注 |

### 4.8 数据与设计

| 表 | 用途 |
| --- | --- |
| `dataDesignProjects` | 数据或设计项目、目标、来源和状态 |
| `dataDesignDeliverables` | 交付物、版本和交付状态 |

### 4.9 学习计划

| 表 | 用途 |
| --- | --- |
| `learningItems` | 课程、书籍、技能和学习提纲 |
| `learningSessions` | 学习日期、时长和进度 |
| `learningNotes` | 笔记、问题和复习状态 |

### 4.10 今日计划与模块的关系

不建立一份复制所有任务内容的通用任务表。今日计划通过“计划适配器”读取各模块中已经安排到日期的行动，并统一转换成如下显示结构：

- 来源模块。
- 来源记录 ID。
- 标题和简短说明。
- 计划日期、开始时间和结束时间。
- 优先级和状态。
- 打开来源记录的路径。
- 完成、重开、延期和修改优先级所需的操作。

各模块自己保存完整业务数据。今日计划只负责聚合和调用来源模块的更新操作，避免同一行动保存两份状态。

### 4.11 每个模块必须实现的统一接口

每个可以进入今日计划的模块必须提供以下能力：

- `listPlanned`：读取指定日期范围的行动。
- `schedule`：把行动安排到某一天。
- `reschedule`：修改计划日期。
- `complete`：完成行动。
- `reopen`：重新打开行动。
- `getSourceRoute`：跳转到来源记录。
- `getSummary`：提供首页摘要。
- `deleteImpact`：说明删除来源记录会影响哪些今日事项。

没有行动概念的模块，例如纯游戏收藏，也必须提供 `getSummary`，但可以不提供完整的计划操作。

## 5. 备份文件设计

### 5.1 备份文件结构

备份必须是单个结构化文件，第一版采用 JSON。逻辑结构如下：

| 字段 | 内容 |
| --- | --- |
| `appId` | 固定应用标识，用于拒绝其他应用的备份 |
| `schemaVersion` | 数据结构版本 |
| `exportedAt` | 导出时间 |
| `appVersion` | 导出时的应用版本 |
| `counts` | 各表记录数量，用于恢复前展示 |
| `data` | 所有本地表的完整数据 |

### 5.2 导出流程

1. 读取 IndexedDB 中的所有数据表。
2. 组装备份对象。
3. 校验备份对象可以完整序列化。
4. 生成带日期和时间的文件名。
5. 下载到用户明确选择或浏览器默认的本机下载位置。
6. 显示导出成功和记录数量。

### 5.3 恢复流程

1. 用户选择本应用导出的 JSON 文件。
2. 读取并解析文件。
3. 校验 `appId`、`schemaVersion`、字段结构和表数据。
4. 展示导出时间、应用版本和记录数量。
5. 明确提示恢复会替换当前全部数据。
6. 用户二次确认后，在一个数据库事务中清空并写入新数据。
7. 任何一步失败都回滚，当前数据保持原样。
8. 恢复成功后刷新界面并显示结果。

### 5.4 必须覆盖的备份测试

- 空数据导出和恢复。
- 每个模块都有数据时的导出和恢复。
- 中文、换行、较长文本的正常保存。
- 取消恢复后数据不变。
- 错误应用标识、缺失字段、损坏 JSON 和未来版本均被拒绝。
- 恢复后刷新和重开浏览器数据仍然存在。

## 6. 开发阶段和具体任务

### 阶段 0：环境和持久化验证

目标：在写业务功能前确认本机存储方案真实可用。

任务：

- `P0-01` 确认目标浏览器为最新 Chrome 或 Edge。
- `P0-02` 验证静态网页打开方式是否可以稳定使用 IndexedDB。
- `P0-03` 如果静态方式不可靠，验证本机回环地址启动方案。
- `P0-04` 初始化 React、TypeScript、Vite、代码检查和测试工具。
- `P0-05` 建立十个一级路由和统一空页面。
- `P0-06` 建立桌面布局基准尺寸：1280x720、1440x900。
- `P0-07` 加入网络请求审计，确认页面不请求外部资源。

交付物：

- 可启动的空白应用骨架。
- 持久化方案验证记录。
- 十个占位页面。
- 格式检查、构建、单元测试和端到端测试命令。

通过门槛：

- 页面可以打开，十个导航入口都可进入。
- 写一条测试记录后刷新、关闭并重开仍然存在。
- 页面只加载本机资源。

### 阶段 1：本地数据库和仓储层

目标：先建立所有模块共用的数据基础，避免后续重复改造。

任务：

- `P1-01` 建立 Dexie 数据库和 `schemaVersion: 1`。
- `P1-02` 建立第 4 节中的全部数据表。
- `P1-03` 为每张表定义 TypeScript 类型和状态枚举。
- `P1-04` 建立按模块划分的仓储，统一新增、编辑、删除和查询行为。
- `P1-05` 建立数据库迁移框架，准备未来升级。
- `P1-06` 建立备份对象、版本校验和恢复事务。
- `P1-07` 使用 fake-indexeddb 编写仓储测试。
- `P1-08` 编写刷新和重开浏览器的持久化烟雾测试。

交付物：

- 本地数据库。
- 模块仓储接口。
- 数据库版本迁移机制。
- 可被后续模块复用的备份和恢复核心服务。

通过门槛：

- 所有表可以正常新增、读取、修改和删除。
- 删除确认逻辑可复用。
- 损坏数据不会导致整个应用白屏。
- 迁移测试和持久化测试通过。

### 阶段 2：公共界面和应用外壳

目标：建立所有模块一致的桌面框架，但保留模块自己的业务视图。

任务：

- `P2-01` 完成左侧固定导航和顶部工具栏。
- `P2-02` 完成应用标题、当前日期、状态提示和全局反馈。
- `P2-03` 建立通用按钮、表单、抽屉、弹窗、确认框、标签和空状态。
- `P2-04` 建立通用筛选栏和列表页头。
- `P2-05` 建立统一的新增、编辑、删除交互。
- `P2-06` 建立键盘操作、焦点状态和表单错误提示。
- `P2-07` 建立响应式桌面布局，不处理手机布局。

交付物：

- 可复用的应用外壳。
- 公共 UI 组件库。
- 十个路由的统一页面结构。

通过门槛：

- 导航、按钮、表单和弹窗不重叠。
- 键盘可以完成主要表单操作。
- 删除操作必须确认，保存和失败都有反馈。

### 阶段 3：快速备忘、今日计划和首页核心

目标：先完成产品的核心闭环，再用同一个模式扩展各模块。

任务：

- `P3-01` 建立快速备忘新增、查看、编辑、删除和已处理状态。
- `P3-02` 建立今日计划独立事项。
- `P3-03` 建立计划适配器和来源模块注册机制。
- `P3-04` 建立今日计划聚合列表、时间安排和优先级。
- `P3-05` 建立完成、重开、延期和重排操作。
- `P3-06` 建立快速备忘转今日事项。
- `P3-07` 建立快速备忘转模块记录的统一转换入口。
- `P3-08` 建立首页今日计划区、快速备忘区和模块摘要卡框架。
- `P3-09` 建立删除来源记录前的关联影响检查。
- `P3-10` 为闭环流程编写组件测试和端到端测试。

交付物：

- 可用的今日计划。
- 可用的快速备忘。
- 首页核心区域。
- 今日计划与来源模块的双向同步机制。

通过门槛：

- 快速备忘可以转换并正确更新。
- 独立事项可以完成、重开和延期。
- 一个模块行动安排到今日后，不产生两份独立数据。
- 从今日完成时，来源模块同步显示完成。

对应验收：

- `AC-08`、`AC-09`、`AC-10`、`AC-11`、`AC-12`、`AC-13`、`AC-14`、`AC-15`。

### 阶段 4：自媒体模块

目标：用一个完整模块验证“模块专属视图 + 今日计划 + 首页摘要 + 备份”的垂直流程。

任务：

- `M-SM-01` 建立选题、写作、待发布、已发布、复盘看板。
- `M-SM-02` 完成内容新增、编辑、删除和阶段流转。
- `M-SM-03` 完成平台、内容类型、日期和阶段筛选。
- `M-SM-04` 完成计划发布和实际发布记录。
- `M-SM-05` 完成一条内容对应多条表现数据。
- `M-SM-06` 接入首页摘要和今日计划适配器。
- `M-SM-07` 覆盖 `AC-16` 并完成备份恢复测试。

通过门槛：

- 自媒体页面明显不同于普通任务列表。
- 内容能够走完整生命周期。
- 即将发布和待复盘内容能进入首页和今日计划。

### 阶段 5：开发工作和咨询工作

目标：完成两类主要工作模块，并复用已经验证的模块模式。

开发工作任务：

- `M-DEV-01` 建立项目列表和项目详情。
- `M-DEV-02` 建立任务、缺陷、版本阶段和状态流转。
- `M-DEV-03` 建立阻塞、下一步和工作记录。
- `M-DEV-04` 建立按项目、类型、状态、优先级和截止日期筛选。
- `M-DEV-05` 接入首页摘要和今日计划。
- `M-DEV-06` 覆盖 `AC-17`。

咨询工作任务：

- `M-CON-01` 建立客户和咨询项目。
- `M-CON-02` 建立会议和会议行动项。
- `M-CON-03` 建立待跟进、工时和可选费用记录。
- `M-CON-04` 建立客户、项目、日期和状态筛选。
- `M-CON-05` 接入首页摘要和今日计划。
- `M-CON-06` 覆盖 `AC-18`。

通过门槛：

- 开发、咨询记录结构互不相同且符合各自场景。
- 阻塞、待跟进、会议和临期行动能正确出现在首页。
- 删除项目或来源记录时，关联行动处理有明确提示。

### 阶段 6：健身计划、饮食计划和游戏娱乐

目标：完成三个生活场景，避免把它们做成同一套清单。

健身任务：

- `M-FIT-01` 建立每周训练计划。
- `M-FIT-02` 建立训练会话和动作组记录。
- `M-FIT-03` 建立身体指标和近期变化。
- `M-FIT-04` 建立完成和部分完成状态。
- `M-FIT-05` 接入首页摘要和今日计划。
- `M-FIT-06` 覆盖 `AC-19`。

饮食任务：

- `M-DIET-01` 建立按日期和餐次的饮食计划。
- `M-DIET-02` 建立实际饮食记录。
- `M-DIET-03` 建立饮水量。
- `M-DIET-04` 建立购物清单和购买状态。
- `M-DIET-05` 建立计划与实际差异视图。
- `M-DIET-06` 接入首页摘要和今日计划。
- `M-DIET-07` 覆盖 `AC-20`。

游戏任务：

- `M-GAME-01` 建立想玩、正在玩、暂停、已通关和放弃状态。
- `M-GAME-02` 建立游戏资料、评分和感想。
- `M-GAME-03` 建立游玩会话、时长和本次进展。
- `M-GAME-04` 建立状态、平台和最近游玩筛选。
- `M-GAME-05` 接入首页摘要，并避免将普通娱乐项视为逾期工作。
- `M-GAME-06` 覆盖 `AC-21`。

通过门槛：

- 健身能够记录动作组和身体指标。
- 饮食能够区分计划、实际、饮水和购物。
- 游戏能够记录状态、游玩会话、评分和感想。

### 阶段 7：数据与设计和学习计划

目标：完成最后两个知识型模块。

数据与设计任务：

- `M-DD-01` 建立数据或设计项目。
- `M-DD-02` 建立来源、参考链接和备注。
- `M-DD-03` 建立交付物、版本和交付状态。
- `M-DD-04` 建立类型、状态和截止日期筛选。
- `M-DD-05` 接入首页摘要和今日计划。
- `M-DD-06` 覆盖 `AC-22`。

学习任务：

- `M-LRN-01` 建立课程、书籍和技能。
- `M-LRN-02` 建立章节、单元和学习提纲。
- `M-LRN-03` 建立学习会话、时长和进度。
- `M-LRN-04` 建立笔记、问题和复习状态。
- `M-LRN-05` 接入首页摘要和今日计划。
- `M-LRN-06` 覆盖 `AC-23`。

通过门槛：

- 数据与设计可以管理项目、交付物和版本。
- 学习计划可以管理进度、笔记和复习。
- 两个模块均进入首页摘要和今日计划体系。

### 阶段 8：首页汇总、备份恢复和统一联动

目标：完成所有模块之间的真实汇总，并正式交付备份恢复界面。

任务：

- `P8-01` 校验八个专项业务模块的首页摘要排序。
- `P8-02` 校验所有模块安排到今日计划后的行为。
- `P8-03` 完成摘要卡点击并筛选到相关记录。
- `P8-04` 完成无数据、无提醒和部分完成状态。
- `P8-05` 完成数据与备份页面。
- `P8-06` 完成导出、文件选择、校验、二次确认和整份恢复。
- `P8-07` 完成下载文件名、错误提示和数据统计。
- `P8-08` 完成所有数据表的备份覆盖检查。
- `P8-09` 覆盖 `AC-24`、`AC-25`、`AC-26`、`AC-27`、`AC-28`、`AC-29`、`AC-30`。

通过门槛：

- 首页摘要全部来自真实模块。
- 备份包含所有模块数据。
- 合法备份恢复后刷新和重开仍然存在。
- 损坏备份不会破坏当前数据。

### 阶段 9：完整验收和稳定性修复

目标：按 PRD 直接执行全部验收，不只做开发自测。

任务：

- `P9-01` 建立 `AC-01` 至 `AC-32` 验收记录表。
- `P9-02` 每个验收项记录通过、失败、证据和修复状态。
- `P9-03` 执行网络断开测试和外部请求检查。
- `P9-04` 执行刷新、重开页面和重开浏览器测试。
- `P9-05` 执行手动重启电脑后的数据检查。
- `P9-06` 执行完整工作流测试。
- `P9-07` 执行备份、取消恢复和损坏文件恢复测试。
- `P9-08` 检查 1280x720 和 1440x900 桌面布局。
- `P9-09` 检查键盘操作、焦点、文本溢出和错误状态。
- `P9-10` 修复所有阻断级和高优先级问题。
- `P9-11` 执行性能检查，确认多模块数据下页面仍可正常使用。

通过门槛：

- 所有 `AC-01` 至 `AC-32` 通过。
- 无阻断级和高优先级缺陷。
- 无必须联网才能使用的功能。
- 无刷新后数据丢失的已知问题。

### 阶段 10：交付整理

目标：让应用可以长期由本人独立启动和维护。

任务：

- `P10-01` 完成正式构建。
- `P10-02` 验证构建产物不依赖外部资源。
- `P10-03` 写清楚启动、关闭、备份、恢复和更新方式。
- `P10-04` 保存测试命令和最终验收结果。
- `P10-05` 固定稳定版本，不继续加入第一版范围外功能。
- `P10-06` 为下一个版本保留数据迁移和需求清单入口。

通过门槛：

- 按交付说明可以正常打开应用。
- 可以完全脱离外部网络使用。
- 数据目录和备份位置清晰。
- `README` 和 `AC-01` 至 `AC-32` 结果齐全。

## 7. 推荐开发顺序

严格按以下顺序执行：

1. 持久化方案验证。
2. 数据表和仓储层。
3. 应用外壳和公共组件。
4. 快速备忘、今日计划、首页核心。
5. 自媒体垂直模块。
6. 开发工作和咨询工作。
7. 健身、饮食和游戏。
8. 数据与设计和学习。
9. 首页完整汇总。
10. 备份恢复。
11. 全部验收。
12. 构建和交付整理。

执行中不得先做大量没有数据基础的漂亮页面，也不得先实现所有模块再统一补今日计划，否则会产生重复数据模型和大规模返工。

## 8. 测试计划

### 8.1 单元测试

必须覆盖：

- 状态枚举和日期处理。
- 各模块仓储的新增、编辑、删除和筛选。
- 今日计划聚合和排序。
- 完成、重开、延期和优先级修改。
- 快速备忘转换。
- 首页摘要排序。
- 备份结构生成。
- 备份校验、版本判断和恢复事务。
- 数据库迁移。

### 8.2 组件测试

必须覆盖：

- 表单必填项和错误提示。
- 新增、编辑、删除确认和取消。
- 空状态。
- 筛选和清除筛选。
- 模块摘要跳转。
- 今日计划完成和延期操作。
- 快速备忘转换表单。

### 8.3 端到端测试

必须覆盖：

| 流程 | 检查内容 |
| --- | --- |
| 基础启动 | 十个页面可进入，无登录页面和外部请求 |
| 快速备忘闭环 | 新增、编辑、转换、处理和显示 |
| 今日计划闭环 | 独立事项、模块行动、完成、重开和延期 |
| 自媒体 | 选题到发布再到数据记录 |
| 开发工作 | 项目、任务、阻塞、工作记录和筛选 |
| 咨询工作 | 客户、项目、会议、行动项、跟进和工时 |
| 健身计划 | 训练安排、动作记录和身体指标 |
| 饮食计划 | 计划、实际、饮水和购物 |
| 游戏娱乐 | 状态、游玩会话、评分和感想 |
| 数据与设计 | 项目、来源、交付物和版本 |
| 学习计划 | 资料、进度、笔记和复习 |
| 持久化 | 刷新、关闭页面、重开同一浏览器配置 |
| 备份恢复 | 导出、修改、恢复、取消恢复和损坏文件 |
| 网络隔离 | 断开互联网后仍能使用全部本机功能 |
| 视觉 | 关键页面在 1280x720 和 1440x900 下无遮挡 |

### 8.4 手工验收

以下项目必须手工检查：

- 重启电脑后数据仍然存在。
- 用目标电脑的常用浏览器正常打开。
- 所有表单可以只用键盘完成主要操作。
- 长标题、长备注和中文文本不会破坏布局。
- 下载的备份文件可以重新选择并恢复。
- 所有 `AC-01` 至 `AC-32` 项均有明确结果。

## 9. PRD 验收项追踪

| 开发阶段 | 对应验收项 |
| --- | --- |
| 阶段 0 和 2 | `AC-01`, `AC-02`, `AC-03`, `AC-31` |
| 阶段 1 | `AC-04`, `AC-05`, `AC-06`, `AC-07` |
| 阶段 3 | `AC-08` 至 `AC-15` |
| 阶段 4 | `AC-16` |
| 阶段 5 | `AC-17`, `AC-18` |
| 阶段 6 | `AC-19`, `AC-20`, `AC-21` |
| 阶段 7 | `AC-22`, `AC-23` |
| 阶段 8 | `AC-24`, `AC-25`, `AC-26`, `AC-27`, `AC-28`, `AC-29`, `AC-30` |
| 阶段 9 和 10 | `AC-01` 至 `AC-32` 全量复验和 `AC-32` 完整流程 |

验收结果必须单独记录，不得只勾选“测试通过”。每项至少记录：

- 验收项编号。
- 测试日期。
- 测试环境。
- 操作步骤。
- 实际结果。
- 使用的截图、日志或备份文件。
- 是否通过。
- 缺陷编号和修复结果。

## 10. 模块完成模板

每个模块开始和结束时都使用同一份检查清单：

1. 页面用途与该模块场景一致。
2. 数据表和类型完成。
3. 新增、查看、编辑、删除完成。
4. 状态或阶段流转完成。
5. 为该模块设计的筛选和视图完成。
6. 空状态和错误状态完成。
7. 首页摘要完成。
8. 今日计划适配完成，或明确说明该模块不需要。
9. 备份导出和恢复覆盖完成。
10. 单元、组件和端到端测试完成。
11. 对应 PRD 验收项通过。
12. 没有引入范围外功能。

## 11. 主要风险和应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 浏览器本地存储不稳定或被清理 | 数据丢失 | 阶段 0 验证方案；使用持久化存储；提供导出备份和风险提示 |
| 静态文件打开后 IndexedDB 行为不一致 | 刷新后数据不可用 | 先做技术验证；必要时改用本机回环地址启动器 |
| 今日计划和模块各存一份状态 | 状态不一致、重复维护 | 只保存模块来源数据，今日计划使用适配器聚合 |
| 所有模块被做成同一种任务表 | 不符合 PRD | 每个模块使用独立数据表、状态、视图和操作 |
| 备份恢复中途失败 | 当前数据损坏 | Zod 校验加数据库事务，失败时完全回滚 |
| 模块过多导致前期结构错误 | 后期大量返工 | 先完成自媒体垂直模块，再批量复制经过验证的架构 |
| 日期在跨时区或半夜出现偏移 | 今日事项显示到错误日期 | 计划日期使用本地 `YYYY-MM-DD`，统一转换函数 |
| 页面逐步变大后首页变慢 | 使用体验下降 | 摘要按需查询、列表分页或限制条数、避免全表渲染 |
| 第一版范围不断扩张 | 无法验收 | 严格执行 PRD 第 9 节暂时不做的功能 |
| 只做自动测试没有真实重开验证 | 持久化假通过 | 自动测试之外必须执行关闭浏览器和重启电脑的手工验收 |

## 12. 每次提交或阶段结束必须执行的检查

未来进入开发后，每个阶段结束至少执行：

```text
格式检查
静态检查
单元测试
组件测试
端到端测试
生产构建
本机打开构建结果
关键数据持久化烟雾测试
相关 PRD 验收项
```

具体命令将在项目初始化时写入 `package.json` 和 `README.md`。在项目建立前不创建这些命令或代码文件。

## 13. 开始开发的确认条件

开始执行阶段 0 前需要确认：

- 同意未来应用放在 `personal-workbench` 子目录。
- 同意使用 React、TypeScript、Dexie、Ant Design 和 Playwright。
- 同意第一阶段先验证 IndexedDB，再开始业务页面。
- 同意先完成快速备忘、今日计划和首页核心，再批量开发八个专项业务模块。
- 同意第一版严格按 `PRD.md` 的“暂时不做”范围执行。
